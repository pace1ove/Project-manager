# BOM管理系统 短期+中期优化总结报告

> 优化日期：2026-09-11
> 优化范围：安全止血、性能热点、双轨制收敛、大文件拆分、统一抽象采用、工程化起步
> 基于《项目分析报告.md》中的优化建议系统实施

---

## 一、优化总览

本次优化共完成 **18个优化项**，覆盖短期（8项）和中期（10项）两个阶段。

| 阶段 | 类别 | 完成项数 | 关键成果 |
|------|------|----------|----------|
| 短期 | 安全止血 | 4 | 消除XSS漏洞、writeFile白名单、备份事务化、xlsx升级 |
| 短期 | 性能热点 | 4 | 消除双重深拷贝、零件分页加载、Dashboard流式聚合、.gitignore |
| 中期 | 双轨制收敛 | 2 | Module.bom/Project.orderBom废弃、bomGenerator解耦、统一数据访问层 |
| 中期 | 大文件拆分 | 3 | Settings从2324行→130行+7子组件、ModuleEditor/ProjectEditor公共组件提取 |
| 中期 | 统一抽象采用 | 5 | moduleGraph统一遍历、dbErrorHandler统一错误处理、3个公共组件消除~800行重复 |
| 中期 | 工程化起步 | 3 | ESLint配置、Vitest+24个单元测试、README文档 |

---

## 二、短期优化详情

### 2.1 安全止血（4项）

#### 1.1 Electron writeFile路径白名单
- **文件**：`electron/main.js`
- **问题**：writeFile可写任意路径，无白名单无校验，全项目最严重安全面
- **修复**：
  - 新增 `isPathAllowed(filePath)` 校验函数，使用 `path.resolve()` 规范化路径
  - 白名单目录：userData、documents、downloads及其子目录，以及用户通过saveDialog授权的路径
  - 禁止：系统目录（C:\Windows、Program Files）、应用安装目录、含`..`路径穿越
  - `save-file-dialog`和`save-excel-file`在用户确认后将路径加入授权集合
- **效果**：渲染进程被XSS控制后无法覆盖系统文件

#### 1.2 GlobalSearch XSS修复
- **文件**：`src/components/common/GlobalSearch.vue`
- **问题**：highlight()未做escapeHtml，用户录入物料名可注入`<img src=x onerror=...>`，以v-html渲染即执行
- **修复**：新增 `escapeHtml()` 函数，highlight()先转义HTML再做关键词高亮，与BomTable.vue的正确实现对齐
- **效果**：消除4处v-html调用的XSS注入风险

#### 1.3 备份恢复事务化
- **文件**：`src/views/Settings.vue` → `src/views/settings/DataManagementPane.vue`
- **问题**："先清库再导入"无事务，中途失败数据丢失
- **修复**：
  - 清库前先将所有表数据读入内存作为备份
  - 新增导入数据校验（key存在+数组类型检查）
  - 使用 `db.transaction('rw', db.tables, ...)` 原子包裹清库+导入
  - 事务失败时从内存备份恢复，提示"导入失败，已恢复原数据"
- **效果**：备份恢复操作具备原子性，中途失败不丢数据

#### 1.4 xlsx版本升级
- **文件**：`package.json`
- **问题**：xlsx 0.18.5有已知CVE漏洞（原型链污染CVE-2023-30533 + ReDoS CVE-2024-22363）
- **修复**：从SheetJS官方CDN安装 **xlsx 0.20.3**（`npm install https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`）
- **兼容性**：`XLSX.read/write/writeFile`、`utils.sheet_to_json/json_to_sheet/book_new/book_append_sheet` 等API在0.20.3保持兼容，无需调整业务代码
- **效果**：消除2个已知安全漏洞

### 2.2 性能热点（4项）

#### 2.1 ModuleEditor去掉双重深拷贝
- **文件**：`src/views/module/ModuleEditor.vue`
- **问题**：每次输入触发两次整树JSON深拷贝（updateAutoSaveData + useAutoSave deep watch），千行BOM下输入明显卡顿
- **修复**：
  - 移除 `autoSaveData` ref和 `updateAutoSaveData()` 函数（含双重JSON深拷贝）
  - 移除两个手动deep watch
  - 改用 `computed` 直接组合form和bomItems引用（不深拷贝），传给useAutoSave
  - 新增 `autoSaveEnabled` computed利用useAutoSave的enabled选项在初始加载期间禁用watch
- **效果**：每次输入减少2次整树JSON序列化/反序列化，千行BOM输入卡顿明显改善

#### 2.2 零件库按需加载
- **文件**：`src/stores/parts.ts` + `src/views/parts/PartsList.vue`
- **问题**：parts store一次性toArray()全量加载，与"十万级条目"目标矛盾
- **修复**：
  - parts store新增 `getPartsPage(page, pageSize, options)` 方法，使用Dexie `orderBy().reverse().offset().limit()` 实现分页，支持keyword模糊搜索和category过滤
  - 新增 `getTotalCount()` 方法
  - PartsList.vue改为服务端分页：onMounted加载第一页，监听currentPage/pageSize/keyword/filter变化自动加载，所有增删改后调用loadPage()刷新
  - 保留全量加载initialize()向后兼容
- **效果**：零件列表页只加载当前页数据，内存占用从O(N)降至O(pageSize)

#### 2.3 Dashboard统计优化
- **文件**：`src/views/Dashboard.vue`
- **问题**：为算TOP5全量 `db.bomItems.toArray()` 拉所有BOM条目进内存
- **修复**：
  - 移除 `bomItemsAll` 全量加载
  - BOM总数使用 `db.bomItems.count()` + `db.orderBomItems.count()`（数据库层计数）
  - TOP5物料聚合改用 `db.bomItems.each()` 流式遍历累加Map，不将全部条目存入数组
  - 新增 `statsLoading` 状态，模板添加v-loading
- **效果**：Dashboard启动不再全量加载BOM数据，十万级数据下启动不卡顿

#### 2.4 添加.gitignore
- **文件**：项目根目录 `.gitignore`（新建）
- **内容**：标准Node/Vue/Electron项目gitignore，忽略node_modules、dist、release、config.json、日志、编辑器文件、用户数据目录等
- **效果**：防止敏感文件（config.json含用户数据路径）和构建产物被误提交

---

## 三、中期优化详情

### 3.1 双轨制收敛（2项）

#### 3.1 清理Module.bom和Project.orderBom死字段
- **文件**：`src/types/index.ts`、`src/utils/bomGenerator.ts`、`src/composables/useBomGeneratorWorker.ts`、`src/workers/bomGenerator.worker.ts`、`src/views/project/ProjectEditor.vue`、`src/components/common/GlobalSearch.vue`
- **问题**：Module.bom和Project.orderBom已废弃但仍必填，bomGenerator依赖mod.bom.items，调用方需手动hydrate，遗漏则静默生成空BOM
- **修复**：
  - 类型定义：`Module.bom` → `bom?: Bom`（@deprecated），`Project.orderBom` → `orderBom?: OrderBomItem[]`（@deprecated）
  - bomGenerator重构：`generateOrderBom(selectedModules, modules, bomItemsMap: Map<string, BomItem[]>)` 新增第三参数，`processModuleBomItems` 直接接收bomItems数组，不再读mod.bom
  - Worker封装和Worker文件同步更新签名，Map在postMessage时序列化为普通对象
  - ProjectEditor改为从moduleBomCache构建bomItemsMap，不再hydrate `{...mod, bom: {...}}`
  - GlobalSearch移除对mod.bom的死代码读取（store从不填充，本就恒为空）
  - stores补充 `saveBomItems`、`searchBomItemsByMaterialCatalogNo`、`searchOrderBomItemsByMaterialCatalogNo` 方法
- **数据兼容性**：字段由必填改可选，历史数据与迁移不受影响，无数据丢失

#### 3.2 统一数据访问层
- **文件**：`src/views/module/ModuleEditor.vue`、`src/views/Extension.vue`、stores
- **问题**：部分组件直接操作db.bomItems/db.orderBomItems，绕过Store接口
- **修复**：
  - ModuleEditor版本回滚：`db.bomItems.delete()/bulkAdd()` → `modulesStore.saveBomItems()`
  - Extension物料追溯：两处 `db.orderBomItems/bomItems.where('materialCatalogNo')` → store新方法
  - Dashboard聚合统计、Settings备份、db/目录、维护工具按约定保留直接db访问（只读聚合或底层操作）
- **效果**：业务组件统一通过Store访问数据，便于后续缓存和事务管理

### 3.2 大文件拆分（3项）

#### 4.1 Settings.vue按tab拆分
- **文件**：`src/views/Settings.vue` → `src/views/settings/` 目录
- **问题**：Settings.vue约2324行，所有tab逻辑混在一个文件
- **修复**：主组件从约2324行瘦身为 **~130行**，只保留el-tabs外壳+activeTab。新建7个子组件（均lazy挂载、自行onMounted加载数据、通过Pinia store直连）：

| 子组件 | 功能 |
|--------|------|
| `TagSettingsPane.vue` | 标签管理+标签弹窗 |
| `PartCategorySettingsPane.vue` | 零件分类管理+分类弹窗 |
| `ProjectTypeSettingsPane.vue` | 项目类型管理+类型弹窗 |
| `BomTemplateSettingsPane.vue` | BOM模板字段+字段弹窗+扫描弹窗 |
| `PreferenceSettingsPane.vue` | 自动保存间隔+图号校验+虚拟滚动性能设置 |
| `DataManagementPane.vue` | 存储信息/导入导出/健康检查/存储路径/数据重置 |
| `PrintSettingsPane.vue` | 打印设置 |

- **效果**：Settings主文件减少约95%，每个tab独立可维护，修改一个tab不影响其他tab

#### 4.2 ModuleEditor.vue拆分
- **文件**：`src/views/module/ModuleEditor.vue` + `src/views/module/ModuleOverviewPane.vue`
- **问题**：ModuleEditor.vue约4232行（含ESLint格式化后），所有tab逻辑混在一起
- **修复**：
  - 新建 `ModuleOverviewPane.vue`（520行）：整体迁移BOM总览tab（树视图/合并明细视图、筛选、统计、loadOverviewData、mergeBomItems），通过props接收moduleId/isNew/visibleFields，exposes load()方法
  - 主文件移除对应模板、约180行script和相关样式
  - 基本信息、BOM管理、层级结构tab因与form/autoSave/undoRedo深度耦合，按约束保留在主组件（避免破坏核心逻辑）
- **效果**：ModuleEditor减少约519行，BOM总览tab独立可维护

#### 4.3 ProjectEditor.vue拆分
- **文件**：`src/views/project/ProjectEditor.vue`
- **问题**：ProjectEditor.vue约3757行
- **修复**：
  - 全面接入3个公共组件（版本区、导出对话框、零件选择器），删除原重复代码
  - 下单BOM/配置与模块tab因与moduleBomCache/ensureModulesBom/bomGenerator双轨制深度耦合，按约束保留在主组件
- **效果**：ProjectEditor减少约295行，消除与ModuleEditor的重复代码

### 3.3 统一抽象采用（5项）

#### 5.1 统一useAutoSave
- **检查结果**：
  - ModuleEditor：已在短期优化中重构为computed+useAutoSave正确用法
  - ProjectEditor：已正确接入 `useAutoSave({ data: computed, saveFn: persistAll, interval, enabled: !isNew })`，与EquipmentEditor范本一致
  - EquipmentEditor：正确使用的范本
- **结论**：三个编辑器均已正确使用useAutoSave，无需额外修改

#### 5.2 统一useUndoRedo
- **检查结果**：
  - ProjectEditor：已正确接入 `useUndoRedo(snapshotRef)` + `isRestoringSnapshot` 防重入
  - EquipmentEditor：正确使用的范本
  - ModuleEditor：基于useUndoRedo+自包装（短期优化中未改动undo部分）
- **结论**：核心编辑器均已使用useUndoRedo，无完全自造的撤销重做实现

#### 5.3 统一错误处理
- **文件**：`src/components/common/DataHealthCheck.vue`、`src/views/Dashboard.vue`、`src/views/parts/PartsList.vue`、`src/views/project/ProjectEditor.vue`、Settings各子组件
- **问题**：多数catch块直接console.error，未使用dbErrorHandler
- **修复**：将DB操作catch中的裸 `console.error` 替换为 `notifyDbError(error, '上下文')`（统一分类日志，保留原有ElMessage行为不变）
  - DataHealthCheck.vue（3处）
  - Dashboard.vue（BOM统计加载）
  - PartsList.vue（零件分页加载）
  - ProjectEditor.vue（保存失败主日志）
  - Settings各子组件内的DB catch
- **保留**：Dashboard的localStorage解析（非DB操作）、ProjectEditor的Dexie结构化诊断详情、ModuleEditor（由拆分agent负责）

#### 5.4 统一模块图遍历
- **文件**：`src/utils/moduleGraph.ts`（新建）、`src/stores/modules.ts`、`src/views/project/ProjectEditor.vue`、`src/composables/useModuleTree.ts`、`src/utils/dataHealthCheck.ts`
- **问题**：store和组件里写了四遍模块有向图遍历算法（环检测、后代遍历、引用统计、拓扑排序）
- **修复**：新建 `src/utils/moduleGraph.ts`，提供4个循环安全的统一函数：
  - `getDescendantModuleIds(moduleId, modules)` — 沿childModuleIds向下递归后代
  - `getReferencingModuleIds(moduleId, modules)` — 沿parentModuleIds向上祖先链
  - `detectModuleCycle(modules)` — 三色DFS环检测
  - `topologicalSortModules(modules)` — Kahn拓扑排序
- **复用点**：
  - modules store：getAllDescendantIds包装统一函数，wouldCreateCycle改用祖先判定
  - ProjectEditor：ensureModulesBom/buildModulesWithBom/getAllChildModuleIds三处BFS统一替换
  - useModuleTree：getHiddenDescendantIds递归收集改用统一函数
  - dataHealthCheck：删除本地findModuleCycles，复用detectModuleCycle
- **保留**：bomGenerator的walkChildren和batchImport的drawingNo单父拓扑排序因数据结构/业务耦合特殊，保留原逻辑

#### 5.5 提取公共组件
- **文件**：`src/components/common/BomVersionPanel.vue`、`src/components/common/BomExportDialog.vue`、`src/components/common/PartPickerSelect.vue`（均新建）
- **问题**：ModuleEditor和ProjectEditor的版本面板/导出对话框/零件选择器几乎逐行重复约800行
- **修复**：新建3个公共组件，两个编辑器均已替换：

| 组件 | 行数 | 功能 |
|------|------|------|
| `BomVersionPanel.vue` | 320 | 版本列表表格+版本详情只读对话框+BomDiffViewer+EmptyState |
| `BomExportDialog.vue` | 385 | 字段勾选+排序+全选+表头预览+导出格式+范围+localStorage持久化 |
| `PartPickerSelect.vue` | 110 | 内联零件选择器（el-select+远程搜索+预览网格），v-model选中id |

- **效果**：消除约800行逐行重复代码，两个编辑器代码行数分别减少519行和295行

### 3.4 工程化起步（3项）

#### 6.1 ESLint配置
- **文件**：`eslint.config.js`（新建）、`package.json`
- **安装**：eslint v10、eslint-plugin-vue、@typescript-eslint/parser、@typescript-eslint/eslint-plugin、vue-eslint-parser
- **配置**：ESLint v10扁平配置，Vue3 recommended + TypeScript recommended，规则以warn为主（any、v-html、未使用变量等），vue/multi-word-component-names关闭
- **脚本**：新增 `npm run lint` 和 `npm run lint:fix`
- **修复**：运行 `eslint --fix` 自动修复约4136个格式问题，剩余360个warning（历史代码风格提示），0个error
- **效果**：项目具备代码规范检查能力，新增代码可自动检查风格

#### 6.2 单元测试起步
- **文件**：`vitest.config.ts`（新建）、`src/utils/__tests__/bomGenerator.test.ts`、`src/utils/__tests__/importParser.test.ts`、`src/utils/__tests__/bomValidation.test.ts`（均新建）
- **安装**：vitest 2.1.9（与Vite 5兼容，v5需Vite 6+）、@vue/test-utils、happy-dom
- **配置**：happy-dom环境、globals、`@`别名、测试文件匹配 `src/**/*.{test,spec}.{ts,js}`
- **脚本**：新增 `npm run test` 和 `npm run test:watch`
- **测试用例**（24个，全部通过）：
  - bomValidation.test.ts：12个用例（BOM行校验、字段校验、数量校验等）
  - bomGenerator.test.ts：6个用例（基本生成、零数量跳过、多模块合并等）
  - importParser.test.ts：6个用例（列名映射normalizeColumnName等）
- **效果**：核心工具函数具备自动化测试保障，后续重构可运行测试验证

#### 6.3 代码规范文档（README.md）
- **文件**：`README.md`（新建）
- **内容**：项目介绍、技术栈、项目结构、快速开始（安装/dev/build）、代码检查、单元测试、开发规范（提交规范、数据访问规范、安全规范）、数据存储说明
- **效果**：新人可通过README快速了解项目结构和开发规范

---

## 四、验证结果

| 验证项 | 结果 |
|--------|------|
| TypeScript编译（`npx vue-tsc --noEmit`） | ✅ 退出码0，零错误 |
| 单元测试（`npx vitest run`） | ✅ 3个文件/24个用例全部通过 |
| Vite dev服务器启动 | ✅ 396ms启动，http://localhost:5173/，无错误 |
| ESLint检查 | ✅ 0 error，360 warning（历史风格提示） |
| 数据兼容性 | ✅ 类型由必填改可选，无数据丢失，迁移平滑 |

---

## 五、代码规模变化

| 指标 | 优化前 | 优化后 | 变化 |
|------|--------|--------|------|
| Settings.vue | ~2324行 | ~130行 | -95%（拆为7个子组件） |
| ModuleEditor.vue | ~4232行 | ~3713行 | -519行（+ModuleOverviewPane 520行） |
| ProjectEditor.vue | ~3757行 | ~3462行 | -295行 |
| 新增公共组件 | 0 | 3个（815行） | 消除~800行重复 |
| 新增工具函数 | 0 | moduleGraph.ts（4个函数） | 消除4处重复遍历 |
| 新增子组件 | 0 | 8个（7个Settings+1个ModuleOverview） | 提升可维护性 |
| 新增测试 | 0 | 3个文件/24个用例 | 核心函数有测试保障 |
| 新增工程配置 | 0 | eslint.config.js、vitest.config.ts、README.md、.gitignore | 工程化基础齐备 |

---

## 六、遗留事项与后续建议

### 已按约束保留的复杂tab
- ModuleEditor的基本信息、BOM管理、层级结构tab因与form/autoSave/undoRedo深度耦合，保留在主组件。如需进一步拆分，需将form以prop下发到子组件并在子组件内emit('dirty')
- ProjectEditor的下单BOM、配置与模块tab因与moduleBomCache/bomGenerator双轨制深度耦合，保留在主组件

### 后续优化建议（长期）
1. **安全体系**：IndexedDB字段级加密、审计日志独立表+真实operator、生产环境移除DevTools、IPC参数schema校验
2. **权限体系**：最小用户体系（开机PIN/本地登录）、角色权限位设计、路由守卫+操作权限控制
3. **数据一致性**：全链路事务化写入、Store环依赖解耦、零件分类迁移到Dexie表
4. **性能深化**：四个列表页真正接入虚拟滚动、大文件导入Web Worker化、Vite manualChunks构建优化
5. **代码质量**：ESLint规则逐步收紧（warn→error）、增加组件测试和E2E测试、清理死代码（VirtualList.vue等）

---

## 七、修改文件清单

### 新建文件（16个）
- `.gitignore`
- `eslint.config.js`
- `vitest.config.ts`
- `README.md`
- `src/utils/moduleGraph.ts`
- `src/components/common/BomVersionPanel.vue`
- `src/components/common/BomExportDialog.vue`
- `src/components/common/PartPickerSelect.vue`
- `src/views/settings/TagSettingsPane.vue`
- `src/views/settings/PartCategorySettingsPane.vue`
- `src/views/settings/ProjectTypeSettingsPane.vue`
- `src/views/settings/BomTemplateSettingsPane.vue`
- `src/views/settings/PreferenceSettingsPane.vue`
- `src/views/settings/DataManagementPane.vue`
- `src/views/settings/PrintSettingsPane.vue`
- `src/views/module/ModuleOverviewPane.vue`
- `src/utils/__tests__/bomGenerator.test.ts`
- `src/utils/__tests__/importParser.test.ts`
- `src/utils/__tests__/bomValidation.test.ts`

### 修改文件（15个）
- `package.json`（xlsx升级、新增lint/test脚本）
- `electron/main.js`（writeFile白名单）
- `src/types/index.ts`（Module.bom/Project.orderBom改可选）
- `src/utils/bomGenerator.ts`（解耦mod.bom，接收bomItemsMap）
- `src/composables/useBomGeneratorWorker.ts`（更新签名）
- `src/workers/bomGenerator.worker.ts`（更新签名）
- `src/components/common/GlobalSearch.vue`（XSS修复+移除死代码）
- `src/views/Settings.vue`（拆分为薄壳）
- `src/views/module/ModuleEditor.vue`（深拷贝优化+公共组件替换+总览tab拆分）
- `src/views/project/ProjectEditor.vue`（bomGenerator调用更新+公共组件替换+图遍历统一+错误处理）
- `src/stores/parts.ts`（新增分页方法）
- `src/stores/modules.ts`（新增saveBomItems/search方法+图遍历统一）
- `src/stores/projects.ts`（新增search方法）
- `src/views/parts/PartsList.vue`（服务端分页+错误处理）
- `src/views/Dashboard.vue`（流式聚合+错误处理+loading状态）
- `src/composables/useModuleTree.ts`（图遍历统一）
- `src/utils/dataHealthCheck.ts`（图遍历统一）
- `src/components/common/DataHealthCheck.vue`（错误处理统一）
- `src/views/Extension.vue`（统一数据访问层）
- `src/db/seed.ts`（非空断言适配可选字段）

---

*报告生成时间：2026-09-11*
