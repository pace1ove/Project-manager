# BOM管理系统 全面分析与优化建议报告

> 分析日期：2026-09-11
> 分析范围：设备/配置/组件/零件层级结构、文件结构、代码逻辑、文件管理、权限安全、工程化、UI/UX
> 分析方法：基于实际源码逐文件通读，三个维度并行深度分析后整合

---

## 一、项目整体架构概述

### 1.1 技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | Vue 3.4 + TypeScript 5.4 + Vite 5.2 |
| UI组件库 | Element Plus 2.6 |
| 状态管理 | Pinia 2.1 |
| 路由 | Vue Router 4.3 |
| 本地数据库 | Dexie 4.4 (IndexedDB) |
| Excel处理 | xlsx 0.18.5 |
| 桌面端 | Electron 44 + electron-builder 26 |
| 工具库 | dayjs |

### 1.2 业务层级结构

```
Equipment（设备）
  ├──< EquipmentConfiguration（配置）        [1:N, equipmentId]
  │        └──< Module（组件/模块）          [N:N 双向冗余: moduleIds ↔ configurationIds]
  │
  ├──< EquipmentSerial（序列号）            [1:N]
  │
  └──< Project（项目）                      [N:1, equipmentId]
           ├── configCombinations[]（多配置组合：配置ID+套数）
           ├── selectedModules[]（选中模块+数量）
           └── orderBomItems（下单BOM条目，独立表）

Module（组件）
  ├──< Module（父子组件）                   [N:N 双向冗余: parentModuleIds ↔ childModuleIds]
  └──< BomItem（组件BOM条目）               [1:N, bomItems.moduleId]
           └──> Part（全局零件库）          [N:1, bomItems.partId]
```

### 1.3 源码规模

- 源文件总数：约 70 个（.ts + .vue）
- 最大文件：ModuleEditor.vue（3312行）、ProjectEditor.vue（3159行）
- 数据库版本：v1→v9，共9次schema迁移
- Store数量：9个（equipment, modules, parts, projects, bomTemplates, bomVersions, tags, projectTypes, user）
- Composable数量：13个
- 工具函数：14个文件

### 1.4 架构亮点

1. **DB迁移规范**：9个版本的upgrade逻辑完整，含幂等性设计和注释
2. **全局零件库设计**：v8引入物料主数据与BOM用量分离，符合主数据管理思路
3. **基础能力齐备**：Worker、虚拟滚动、错误分类、空/加载/错误三态组件、变更历史、版本快照均有实现
4. **路由懒加载**：所有页面路由均为动态import
5. **Electron安全基线**：contextIsolation:true + nodeIntegration:false 已正确配置
6. **导入链路完整**：模板下载→解析→字段映射→校验→拓扑排序→冲突处理→落库→错误报告

---

## 二、各维度详细分析

### 2.1 业务层级与数据模型

#### 2.1.1 核心结构性矛盾："双轨制"并存

项目经历v4→v9多次演进，遗留了三套"事实来源"并存的问题，是所有数据不一致的总根源：

| 实体 | 内嵌字段（已废弃） | 独立表（实际使用） | 状态 |
|------|-------------------|-------------------|------|
| 组件BOM | `Module.bom: Bom`（types:172，必填） | `bomItems` 表 | store从不填充bom，类型与持久化矛盾 |
| 下单BOM | `Project.orderBom: OrderBomItem[]`（types:247，必填） | `orderBomItems` 表 | 实际读写均走独立表 |
| 零件分类 | `PartCategory` 类型定义 | localStorage（`bom_part_categories`） | 无IndexedDB表，跨存储无事务 |

**影响**：`bomGenerator.ts:209` 依赖 `mod.bom.items`，但modules store从不填充该字段，调用方需手动hydrate，遗漏则静默生成空BOM。

#### 2.1.2 类型定义问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| D1 | 高 | types/index.ts:172 | `Module.bom` 必填但实际从不填充，应标deprecated并改为可选 |
| D2 | 高 | types/index.ts:247 | `Project.orderBom` 必填但已废弃，误导新代码 |
| D3 | 高 | types/index.ts:132-155 | `BomItem` 重复声明约15个本属Part的字段（drawingNo, jobNo, chineseDescription等），类型与实际写入字段集不符 |
| D4 | 中 | types/index.ts:128,154,214 | Part/BomItem/OrderBomItem三处均有 `[key: string]: any`，等于关闭核心实体字段级类型检查 |
| D5 | 中 | types/index.ts:240 | `Project.configurationId` 注释"兼容旧版本"但类型为必填非空，实际会被置为'' |
| D6 | 中 | types/index.ts:151 vs 110 | `BomItem.type` 与 `Part.partType` 是同一枚举的两份拷贝，靠as断言同步 |
| D7 | 中 | types/index.ts:287 + usePartCategories.ts:3 | PartCategory接口重复定义两份 |
| D9 | 低 | types/index.ts:213 | `isModuleItem` 仅在OrderBomItem声明，BomItem靠as any读取 |

#### 2.1.3 数据库设计问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| DBB1 | 高 | db/index.ts:332-359 + usePartCategories.ts | **零件分类没有IndexedDB表**，存于localStorage。v9迁移写硬编码ID(cat_85等)，用户重命名/删除分类后零件库partCategory成悬空引用，跨存储无事务无级联 |
| DBB2 | 高 | db/index.ts:286-299 + parts.ts:377-389 | **空图号零件每条独立创建**，永不去重，长期运行parts表膨胀 |
| DBB3 | 高 | db/index.ts:219-221 vs 183 | **v6增补的bomItems.drawingNo索引在v8被静默删除**（Dexie全量替换索引串机制），零件追溯按图号查询的设计意图被覆盖 |
| DBB4 | 中 | parts.ts:328-421 | **重同步先clear再重建，partId全部重新生成**，changeHistory/BomVersion快照中的partId全部失效；用户手动新建未被引用的零件被一并清空 |
| DBB5 | 中 | db/index.ts:230 + parts.ts:12 + batchImport.ts:798 | **BOM_ONLY_FIELDS三份拷贝**，字段集靠人工保持一致 |
| DBB6 | 中 | db/index.ts:190-211 | v7父子关系迁移在for循环内逐模块put，非批量 |
| DBB7 | 中 | db/index.ts:173-175 | bomVersions仅有targetId单字段索引，缺[targetType+targetId]复合索引 |
| DBB10 | 低 | db/index.ts:380-396 | clearAllTables手工列举13张表，后续加表易漏 |

#### 2.1.4 Store设计问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| S1 | 高 | modules.ts + equipment.ts + projects.ts | **三个核心store环依赖**，靠函数体内延迟调用化解，加载顺序变动即触发循环引用 |
| S2 | 高 | parts.ts:93-95 + 各store initialize() | **全量加载策略与"十万级条目"目标矛盾**，parts一次性toArray()全量载入，零件是增长最快的实体 |
| S3 | 中 | modules.ts（721行） | **职责过载**：CRUD + 环检测 + 级联删除 + BOM条目CRUD + 零件合并 + 复制 |
| S4 | 中 | modules.ts:54,59,353-382 | **fire-and-forget写入无补偿**：addModule先push内存再DB写入，父模块childModuleIds同步更新后若DB写入失败无回滚；copyModule递归复制失败仅console.error |
| S5 | 中 | modules.ts:592-641 | **零件参数/BOM字段拆分靠硬编码白名单**，用户改BOM行私有字段会被广播到所有共享该零件的模块 |
| S6 | 中 | projects.ts:208-234 | **生成下单BOM非事务**：先delete再bulkPut，两步之间崩溃=下单BOM丢失 |
| S7 | 中 | modules.ts:499-507 | 严格模式下查不到part时静默回退到过期内联数据，无告警 |

#### 2.1.5 核心业务逻辑问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| L1 | 高 | bomGenerator.ts:209-211 + useBomGeneratorWorker.ts:42 | **下单BOM生成依赖未契约化的mod.bom.items**，modules store从不填充，调用方需手动挂载，遗漏则静默空BOM |
| L2 | 中 | bomGenerator.ts:115-120,324-332 | moduleFirstSeen/moduleCountMap为死代码；configCombinations的套数未真正参与生成计算 |
| L3 | 中 | bomGenerator.ts:277,310 + batchImport.ts:1004 | fixedFields黑名单在函数内复制两遍+导入处第三份，字段增删易漏改 |
| L5 | 中 | batchImport.ts:301-319,884 | **批量导入不支持多父组件**，parentDrawingNo单值，与运行时已支持的多父组件能力不对齐 |
| L6 | 中 | batchImport.ts:47,207-208 | **"图号/drawingNo"双重语义混淆**：BOM Sheet里drawingNo被当作关联模块，而ValidBomItem.drawingNo又是条目自身图号 |
| L7 | 低 | bomValidation.ts:81-98 vs bomGenerator.ts:218-225 | 数量校验口径不一致：校验层要求>0拒绝入库，生成层对≤0仅warning跳过 |

---

### 2.2 文件结构与代码逻辑

#### 2.2.1 文件结构问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| FS1 | 高 | src/components/common/ | 16个组件扁平化堆放，BomTable(1131行业务核心)与EmptyState(纯UI原子件)混在一起，无业务分组 |
| FS2 | 高 | src/components/common/VirtualList.vue | **死代码**：全仓库无任何文件import；BomTable内部又手写了一套虚拟滚动(582-628行)，同一能力两套实现 |
| FS3 | 高 | usePerformanceSettings.ts:66 + Settings.vue:1085 + 四个列表页 | **列表虚拟滚动功能半成品**：composable提供阈值判断、Settings提供配置UI，但四个列表页全部走el-table分页，没有一个调用shouldUseListVirtualScroll，给用户一个不生效的开关 |
| FS4 | 中 | src/db/seed.ts:2-7 + stores/bomTemplates.ts:6 | **mock数据未做环境隔离**，被顶层静态import，生产包包含全部演示JSON，首次启动写入真实用户IndexedDB |
| FS5 | 中 | vite.config.ts:5-20 | 无manualChunks，xlsx/element-plus全打进主bundle；main.ts:25-27全量注册所有Element Plus图标 |
| FS6 | 低 | src/views/ | Dashboard/Settings/Extension平铺在views根，与equipment/module等子目录层级不一致 |

#### 2.2.2 大文件拆分方案

| 文件 | 行数 | 拆分建议 |
|------|------|----------|
| **ModuleEditor.vue** | 3312 | 拆为：ModuleBasicPane、ModuleHierarchyPane、ModuleOverviewPane、ModuleBomDialog、BomImportWizard(460行)、BomExportDialog、ModuleVersionPane；主文件可降至~600行 |
| **ProjectEditor.vue** | 3159 | 拆为：ProjectBasicPane、ProjectModuleSelectPane、OrderBomPane、ProjectBomGenerateBar；版本/历史/导出/零件选择器与ModuleEditor高度重复，应抽共享组件 |
| **Settings.vue** | 2324 | 按tab拆6个子组件：TagSettingsPane、PartCategoryPane、BomTemplatePane、DataManagementPane、StoragePathPane、PreferencePane；主文件仅保留el-tabs壳 |
| **ModuleList.vue** | 1815 | 抽useCrudListPage composable封装搜索/分页/列配置/过滤/多选；对话框拆子组件 |
| **PartsList.vue** | 1702 | 同上；编辑/引用/导入对话框拆子组件 |
| **EquipmentEditor.vue** | 1772 | 拆为：EquipmentBasicPane、EquipmentConfigPane、EquipmentModuleTransfer、EquipmentSerialPane、EquipmentHistoryPane（此文件是唯一正确使用useAutoSave/useUndoRedo的范本） |
| **BomTable.vue** | 1131 | 抽useTableVirtualScroll、useCellEdit、useTableFilter三个composable |
| **batchImport.ts** | 886 | 按职责拆：columnMapping、moduleImportParser、partConflictResolver、resultAssembler |
| **modules.ts (store)** | 721 | 有向图算法(cycle检测/后代遍历/引用统计)抽utils/moduleGraph.ts |

#### 2.2.3 组件代码质量问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| CQ1 | 高 | ModuleEditor.vue:1244-1245 | **每次输入触发两次整树JSON深拷贝**：watch form与bomItems(deep)→updateAutoSaveData做两次JSON.parse(JSON.stringify)，useAutoSave又deep watch一次，undoState第三次。千行BOM下输入明显卡顿 |
| CQ2 | 高 | ModuleEditor.vue:1247-1251 | **useAutoSave被"半使用"**：只解构isSaving，dirty/lastSaved/markClean/retry全部自造，导致composable内置的3次重试机制完全失效 |
| CQ3 | 高 | ModuleEditor/ProjectEditor/EquipmentEditor | **undo/redo三种实现并存**：ModuleEditor基于useUndoRedo+自包装，ProjectEditor自造createSnapshot再喂给useUndoRedo，EquipmentEditor直接用useUndoRedo。同一条业务三种快照策略 |
| CQ4 | 中 | ModuleEditor.vue:1250 + ProjectEditor:2492 | 自动保存间隔在setup时求值一次，非响应式，设置页改间隔后已打开编辑器不生效 |
| CQ5 | 中 | router/index.ts + ModuleEditor:3138 | 无404兜底路由，无beforeEach守卫，meta.title已定义但从未设置document.title |
| CQ6 | 中 | BomTable.vue:409-413 | 选择态双源：组件内部管selectedRows Set又对外emit selection-change，父组件还要再clearBomSelection |
| CQ7 | 中 | PartsList.vue:665 | watch 5个状态但搜索词无防抖（ModuleList已用useDebounceRef，PartsList没用，不一致） |
| CQ8 | 中 | ModuleEditor.vue:1134,1667,1768,1704,1712 | 同一批modules在setup里派生5个树/选项结构，任一变化全部重算；且未使用已有的useModuleTree composable |

#### 2.2.4 工具函数与Composables问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| UT1 | 中 | importParser.ts:17 + batchImport.ts:37,13 | **三套列名映射并存**：COLUMN_ALIAS_MAP、buildDynamicBomColumnMap、IMPORT_SPECIAL_FIELDS，"图号/drawingNo"在两处定义，极易漂移 |
| UT2 | 中 | 多个Editor | **应提取未提取的重复逻辑**：版本/变更历史面板(ModuleEditor 2902-3079与ProjectEditor 1912-2059几乎逐行重复)、BOM导出对话框(字段勾选/排序/本地持久化)、零件库选择器、列表页骨架(四个List页复制四份)、模块有向图遍历(同一份算法写了四遍) |
| UT3 | 中 | dbErrorHandler.ts + 各组件 | 错误处理工具设计完善但未被普遍采用，多数catch为console.error或直接ElMessage.error |
| UT4 | 低 | useDebounce.ts:68 | import { watch }写在文件底部，不规范 |

#### 2.2.5 性能问题

| # | 严重度 | 问题 |
|---|--------|------|
| P1 | 高 | ModuleEditor每次输入双份JSON深拷贝（见CQ1），千行级BOM明显卡顿 |
| P2 | 高 | 四个列表页分页而非虚拟化，ModuleList:711 loadAllBomItems一次性把所有模块BOM加载进内存 |
| P3 | 中 | ModuleEditor派生5个模块树结构全部重算（见CQ8） |
| P4 | 中 | useAutoSave的retryTimer在组件卸载时未清理，可能残留 |
| P5 | 低 | MainLayout.vue:241手写resize监听未走useCleanup规范 |

---

### 2.3 文件管理

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| F1 | 高 | Settings.vue:1559-1585 | **备份恢复"先清库再导入"无事务保护**：先clearAllTables清空11张表再逐表bulkPut，中途失败则原数据已毁、新数据不全，等同于数据丢失；导入前未自动生成临时备份 |
| F2 | 中 | importErrorReport.ts:143 | 错误报告导出用XLSX.writeFile浏览器下载，未走Electron保存对话框，与其他导出行为不一致 |
| F3 | 中 | importParser.ts:66 + batchImport.ts:505 | **大文件全量读入内存、无上限、无进度**：FileReader一次性读整个Excel，XLSX.read全量解析，无Web Worker，十万级数据易OOM |
| F4 | 中 | batchImport.ts:47,778-785 | 字段映射分散三处且存在歧义，drawingNo列被强制当作关联组件图号，与BOM明细drawingNo同名冲突 |
| F5 | 中 | electron/main.js:451-506 | 数据迁移是"复制非移动"且不校验完整性，复制失败仅continue，旧目录残留完整数据 |
| F6 | 低 | batchImport.ts:736-791 | BOM明细行级缺重检测，同一组件下同一物料出现多次不报错直接追加 |

---

### 2.4 权限与安全

#### 2.4.1 高危安全问题（★需立即修复）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| S1 ★ | **高** | electron/preload.js:39 + main.js:278-290 | **writeFile可写任意路径，无白名单/无参数校验**：渲染进程调用writeFile(anyPath, anyData)，主进程直接fs.writeFileSync。一旦渲染进程被XSS控制，可覆盖任意文件。**全项目最严重的安全面** |
| S2 ★ | **高** | GlobalSearch.vue:313-318 | **真实XSS漏洞**：highlight()直接text.replace(regex, '<mark>…</mark>')，未先escapeHtml。text来自用户录入的BOM数据(name/chineseDescription/drawingNo)，以v-html渲染。攻击者在物料名称存`<img src=x onerror=…>`，全局搜索触发即执行。对比BomTable.vue:836和Extension.vue:612都正确先转义 |
| S3 ★ | **高** | stores/user.ts:6-9 | **用户系统形同虚设**：硬编码{name:'管理员', role:'admin'}，无登录、无密码、无会话。UserInfo仅name和role，role是裸字符串无权限位 |
| S4 ★ | **高** | router/index.ts:32-35 | **路由零守卫**：无beforeEach，任何页面（含/settings的清空数据、备份恢复）直接可进 |
| S5 ★ | **高** | db/index.ts 全文 | **IndexedDB全明文**：所有BOM、物料、价格(totalAmount)等商业数据裸存，config.json明文写dataPath。共享电脑下任何人用DevTools即可导出全部数据 |
| S6 ★ | **高** | db/index.ts:30 + 全局 | **审计记录未落独立表**：changeRecords表除seed.ts演示数据外，业务运行时从未add/bulkPut。审计只内嵌在各实体changeHistory数组里，删除实体审计一并丢失；operator恒为硬编码"管理员"，无法追责 |

#### 2.4.2 中低危安全问题

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| S7 | 中 | electron/main.js:199 | 生产菜单保留"开发者工具"切换，终端用户可打开DevTools篡改前端状态、直接调IPC |
| S8 | 中 | electron/preload.js:19 + main.js:157-160 | openExternal无URL白名单，可唤起file://、smb:等自定义协议 |
| S9 | 中 | Settings.vue:1482-1531 | 备份文件导入无schema校验，只校验key存在+Array.isArray，数组元素内容/字段类型/外键完整性均不校验 |
| S10 | 中 | electron/main.js:259-548 | IPC参数校验普遍薄弱，多数只判空字符串，不校验类型/长度/路径合法性 |
| S11 | 低 | utils/storage.ts:35 | generateId用Math.random()，非加密安全随机 |

---

### 2.5 工程化

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| E1 ★ | **高** | 项目根目录 | **无.gitignore**：node_modules/、dist/、release/、config.json（含用户数据路径）极易被误提交 |
| E2 ★ | **高** | 项目根目录 | **无ESLint/Prettier/.editorconfig**，package.json无lint脚本，多人协作时代码风格全靠自觉 |
| E3 ★ | **高** | test/目录 + package.json | **没有真正的自动化测试**：test/*.js是手工复制业务逻辑的临时脚本，无断言框架、无test脚本、不随构建运行 |
| E4 ★ | **高** | package.json:25 | **xlsx@^0.18.5已知有安全漏洞**：原型链污染(CVE-2023-30533)与ReDoS(CVE-2024-22363)，且SheetJS已停止在npm发布该线版本 |
| E5 | 中 | vite.config.ts:5-20 | 无manualChunks，element-plus/dexie/xlsx全打进一个chunk，首屏vendor包偏大 |
| E6 | 中 | tsconfig.json:15-16 | 未启用noUnusedLocals/noUnusedParameters，batchImport.ts:710有"算了不用"的残留 |
| E7 | 中 | 项目根目录 | 无README.md、无CHANGELOG，新人接手只能看DEV_LOG/DEBUG_LOG |
| E8 | 低 | package.json:30 | electron@^44.2.0版本号异常（当前公开主线约33-37），需核实是否笔误 |
| E9 | 低 | vite.config.ts:13-19 | 无.env环境变量区分，靠ELECTRON_DEV==='1'字符串判断 |

---

### 2.6 UI/UX

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| U1 | 中 | Dashboard.vue:178-190 | **Dashboard为算TOP5全量加载BOM**：onMounted里db.bomItems.toArray()拉全部条目进内存，十万级数据下启动即卡顿 |
| U2 | 低 | Dashboard.vue | 异步加载期间统计卡片显示0，无Skeleton骨架屏 |
| U3 | 低 | MainLayout.vue:309-330 | "重置演示数据"confirm文案未提示"建议先备份" |
| U4 | 低 | GlobalErrorBoundary.vue:65-70 | "尝试恢复"按钮只清前端错误状态，对DB类错误无效 |

> **正面评价**：EmptyState/SkeletonScreen/GlobalErrorBoundary三个组件质量高，可作为范式；MainLayout侧边栏折叠+三档响应式断点(1200/992/576)，UX基础扎实。

---

## 三、待优化点清单（按优先级排序）

### 🔴 高优先级（安全/数据丢失/核心架构，建议立即处理）

| 编号 | 问题 | 影响范围 | 建议方案 |
|------|------|----------|----------|
| H1 | **writeFile可写任意路径** | Electron安全 | 白名单收敛：只允许写入用户通过showSaveDialog选择的路径，或限制在downloads/数据目录内；对..和系统目录做校验 |
| H2 | **GlobalSearch XSS漏洞** | 安全 | 在highlight()开头加escapeHtml(text)，与BomTable对齐 |
| H3 | **备份恢复无事务保护** | 数据安全 | 改为事务化导入：先写临时表/事务内完成全部put，校验通过后原子替换；导入前自动全量导出到临时文件 |
| H4 | **双轨制：Module.bom/Project.orderBom废弃字段** | 数据一致性 | 标注deprecated→改为可选→长期移除；bomGenerator改为显式接收moduleId→items的Map |
| H5 | **零件分类无IndexedDB表** | 数据一致性 | 新增partCategories Dexie表，删除localStorage实现；删除分类时级联清空零件partCategory |
| H6 | **ModuleEditor每次输入双份JSON深拷贝** | 性能 | 让useAutoSave直接watch form与bomItems，删手工autoSaveData快照；undo只在提交操作时快照 |
| H7 | **重同步重建partId破坏引用** | 数据安全 | 按drawingNo复用已有partId，未被引用的零件保留；清空操作单独拆分加二次确认 |
| H8 | **无.gitignore** | 工程化 | 立即补.gitignore：node_modules、dist、release*、*.log、config.json、BOM管理系统数据/ |
| H9 | **xlsx 0.18.5安全漏洞** | 安全/依赖 | 迁移到SheetJS官方CDH最新版，或换exceljs |
| H10 | **全量加载与"十万级"目标矛盾** | 性能 | parts改按需+索引查询；零件列表页走分页；Dashboard聚合用Dexie each/流式统计 |

### 🟡 中优先级（架构优化/代码质量/功能完善，建议近期处理）

| 编号 | 问题 | 建议方案 |
|------|------|----------|
| M1 | 大文件拆分（ModuleEditor 3312行/ProjectEditor 3159行/Settings 2324行） | 按功能块拆子组件和composable，Settings按tab拆分收益最大风险最低 |
| M2 | ModuleEditor/ProjectEditor重复代码（版本面板/导出对话框/零件选择器） | 抽共享BomVersionPanel、BomExportDialog、usePartPicker，消除约800行重复 |
| M3 | useAutoSave半使用、undo/redo三种实现 | 三个Editor统一采用EquipmentEditor的正确写法 |
| M4 | 三套列名映射并存 | 收敛为utils/import/columnAlias.ts单一数据源 |
| M5 | BOM_ONLY_FIELDS/fixedFields三份拷贝 | 抽全局常量，三处共用 |
| M6 | 下单BOM生成非事务、fire-and-forget无回滚 | 统一用db.transaction包裹内存+DB双写 |
| M7 | 批量导入不支持多父组件 | 导入模板扩展逗号分隔父组件图号，拓扑排序改多父入度计算 |
| M8 | 大文件导入全量读内存 | 加文件大小上限，XLSX.read移入Web Worker，加进度条 |
| M9 | 虚拟滚动半成品/假开关 | 要么四个列表页真正接入，要么删除Settings阈值项 |
| M10 | 无ESLint/Prettier | 引入eslint+eslint-plugin-vue+@typescript-eslint+prettier，加lint脚本 |
| M11 | 无自动化测试 | 引入Vitest，batchImport的纯函数作为首批单测目标 |
| M12 | 生产菜单保留DevTools | Menu.setApplicationMenu前判断!app.isPackaged，生产环境移除 |
| M13 | IndexedDB全明文 | 引入Dexie加密插件或字段级加密，开机口令派生密钥 |
| M14 | 审计记录未落独立表 | 所有写操作统一走recordChange()写入changeRecords表，operator来自真实用户 |
| M15 | 路由零守卫+无404 | 加全局守卫设title，加404兜底，抽useDirtyGuard统一离开拦截 |
| M16 | mock数据未隔离 | 改为动态import仅在空库时加载，生产版提供"是否含演示数据"构建开关 |
| M17 | Vite无构建优化 | manualChunks拆分vendor，图标按需import，xlsx动态import |
| M18 | v6的drawingNo索引在v8丢失 | v10索引串补回drawingNo |
| M19 | 空图号零件永不去重 | 建立指纹(chineseDescription+materialCatalogNo+reserved1哈希)做二级去重 |
| M20 | Store环依赖 | 跨实体引用统计抽到独立service，import全部收敛到函数体内 |

### 🟢 低优先级（规范性/体验优化，可排期处理）

| 编号 | 问题 | 建议方案 |
|------|------|----------|
| L1 | 无README/CHANGELOG | 补README（启动/构建/目录结构/数据路径），发版写CHANGELOG |
| L2 | tsconfig未开noUnusedLocals | 打开两项，配合ESLint清理死代码 |
| L3 | useDebounce import在文件底部 | 移到顶部 |
| L4 | 错误处理未统一走handleDbError | 规定所有db操作catch一律过handleDbError/notifyDbError |
| L5 | Dashboard无加载态 | 包一层SkeletonScreen |
| L6 | generateId用Math.random() | 换crypto.randomUUID() |
| L7 | openExternal无URL白名单 | 仅放行https?:// |
| L8 | IPC参数校验薄弱 | 对每个handler加schema校验 |
| L9 | clearAllTables手工列举 | 改为db.tables.map动态遍历 |
| L10 | electron版本号异常 | 核实并锁定具体次版本 |

---

## 四、优化路线图建议

### 短期（1-2周）：安全止血 + 性能热点

**目标**：消除高危安全漏洞和数据丢失风险，解决最影响使用体验的性能问题。

1. **安全修复**（H1, H2, H9）
   - writeFile路径白名单改造
   - GlobalSearch highlight加escapeHtml
   - xlsx升级或替换

2. **数据安全**（H3, H7）
   - 备份恢复事务化
   - 重同步复用partId

3. **性能热点**（H6, H10）
   - ModuleEditor去掉每次输入的双份深拷贝
   - parts全量加载改按需查询
   - Dashboard TOP5改流式聚合

4. **工程基础**（H8）
   - 补.gitignore

### 中期（1-2月）：架构收敛 + 代码质量

**目标**：消除双轨制，统一抽象采用，拆分大文件，建立工程规范。

1. **双轨制收敛**（H4, H5, M18, M19）
   - Module.bom/Project.orderBom下线计划
   - 零件分类迁移到Dexie表
   - 补回drawingNo索引
   - 空图号零件去重

2. **大文件拆分**（M1, M2）
   - Settings.vue按tab拆6个子组件（风险最低，优先）
   - ModuleEditor/ProjectEditor抽共享组件（版本面板/导出/零件选择器）
   - BomTable抽三个composable

3. **统一抽象**（M3, M4, M5）
   - 三个Editor统一useAutoSave/useUndoRedo用法
   - 列名映射、BOM_ONLY_FIELDS收敛为单一数据源

4. **工程规范**（M10, M11, M16, M17）
   - ESLint+Prettier接入
   - Vitest单测起步
   - mock数据动态import
   - Vite manualChunks优化

5. **功能完善**（M7, M8, M9）
   - 批量导入支持多父组件
   - 大文件导入Worker化
   - 虚拟滚动要么接入要么删除

### 长期（3-6月）：安全体系 + 权限 + 可维护性

**目标**：建立完整的安全和权限体系，提升系统可维护性和可扩展性。

1. **安全体系**（M13, M14, M12, L7, L8）
   - IndexedDB字段级加密
   - 审计日志独立表+真实operator
   - 生产环境移除DevTools
   - IPC参数schema校验
   - openExternal白名单

2. **权限体系**（S3, S4, M15）
   - 最小用户体系（开机PIN/本地登录）
   - 角色权限位设计
   - 路由守卫+操作权限控制

3. **数据一致性**（M6, M20）
   - 全链路事务化写入
   - Store环依赖解耦

4. **文档与规范**（L1, L2, L4）
   - README/CHANGELOG
   - 死代码清理
   - 错误处理统一

---

## 五、总结

### 核心矛盾

本项目的核心矛盾不是"缺能力"，而是**"已有抽象没有被统一采用"**：

- useAutoSave/useUndoRedo设计完善，但ModuleEditor/ProjectEditor绕过或半使用，EquipmentEditor却是正确范本
- 虚拟滚动composable和配置UI都做了，但四个列表页没一个接入
- 错误处理工具dbErrorHandler设计完善，但多数catch直接console.error
- 模块有向图遍历算法在store和组件里写了四遍
- ModuleEditor和ProjectEditor的版本面板/导出对话框/零件选择器几乎逐行重复

### 最值得优先做的三件事

1. **安全止血**：writeFile白名单 + GlobalSearch XSS修复 + 备份恢复事务化（防止数据丢失）
2. **性能体验**：ModuleEditor去掉每次输入的双份JSON深拷贝（千行BOM输入卡顿立竿见影）
3. **架构收敛**：Settings.vue按tab拆分（2324行→主文件<300行，风险最低收益最大）+ ModuleEditor/ProjectEditor抽共享组件（消除约800行重复代码）

### 数据一致性总根源

v4→v9多次演进遗留的"双轨制"（Module.bom内嵌 vs bomItems表、Project.orderBom内嵌 vs orderBomItems表、PartCategory类型 vs localStorage）是所有数据不一致问题的总根源，建议作为中期架构收敛的第一优先级。
